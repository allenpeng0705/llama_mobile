<resources>
    <!-- Base application theme (AppCompat; material attrs were only for the old
         v1 demo UI and were removed with it). -->
    <style name="Theme.AndroidSDKExample" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryDark">@color/purple_700</item>
        <item name="colorAccent">@color/teal_200</item>
    </style>
</resources>
