pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "androidLibExample"
include ':app'
include ':llama_mobile'

project(':llama_mobile').projectDir = new File(rootProject.projectDir, '../../llama_mobile-android')
