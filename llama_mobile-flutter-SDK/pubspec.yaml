---
name: llama_mobile_flutter_sdk
description: llama_mobile v2 Flutter SDK — on-device LLM inference (LlamaEngine).
version: 2.0.0
homepage: https://github.com/llama-mobile/llama_mobile
repository: https://github.com/llama-mobile/llama_mobile
environment:
  sdk: "^3.9.2"
  flutter: ">=3.3.0"
dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: "^2.0.2"
  http: "^1.2.1"
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: "^5.0.0"
flutter:
  plugin:
    platforms:
      android:
        package: com.llamamobile
        pluginClass: LlamaMobileFlutterSdkPlugin
      ios:
        pluginClass: LlamaMobileFlutterSdkPlugin
